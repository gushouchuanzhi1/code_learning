const修饰的指针，在编译的时候会给他开辟一块空间，只要变量内容相同，都只会存储一遍，
而内容相同的字符数组，会存储两遍，arr1不等于arr2
数组名通常表示数组首元素的地址，
但是有例外，sizeof和&arr 表示整个数组，前者计算整个数组的大小，后者表示取数组的地址。
&arr +1 会将整个数组跳过

对于传入函数的形参数组，行可以省略，但是列不能省略！

void print(int (*arra)[5]){ 传入了一个二维数组

}

int(*p)(int,int)这个是用一个指针指向了可以接受两个整型变量。返回值为整性的一个函数







