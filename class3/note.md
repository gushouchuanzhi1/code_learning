操作符
计算式存在一个小数最后结果就需要按照小数来保存
例如float a = 7/2.0;

移位操作符，针对于二进制来说

非零表示真

typedef struct Node{

}Node;
这里的Node相当于 struct Node 给这个类型重新起了一个新的名字

static表示静态的，修饰局部、全局变量或者修饰函数
如果用于修饰变量，那么这个变量会有一个给定的作用域，不会被销毁。见text.c

extern 声明外部变量

