接口是一种规则对行为的一种抽象，比如Person类子类的Student和Teacher都会游泳，Animal类子类的Frog和Dog都会游泳。
如何定义、使用接口，以及里面的成员的特点。public interface name{}
某个类使用public class name implements 接口名{} 在该类中也要重写接口的所有抽象方法

接口作为一种规则，其里面的成员变量只能是常量（可以省略final），不能有构造方法，成员方法只能是抽象方法。
如果接口有重名的现象，同名的方法，最后只要重写一次即可。
接口和接口是继承关系，可以单或多继承。

jdk8中允许接口中定义默认方法，需要使用关键字default来修饰，public default void show(){}
默认方法不算是抽象方法，可以不被重写，但是重写的时候要去掉default关键字
接口中的私有方法只为该接口中的方法服务。












