指针
&a取了a的地址
int a = 4;
int* p = &a;
而*p中的 '*'是解引用 找到这个地址中的内容

实参：
真实传给函数的变量
形参
函数名后括号内的变量

如果想要通过函数交换两个变量的值，需要通过指针来交换

void swap(int* a, int* b) {// * 为解地址符
	int c = 0;
	c = *a;
	*a = *b;
	*b = c;
}

函数的递归
函数定义内又调用其本身
递归和迭代的区别在于，迭代最后停止的时候按照趋近值，即误差值的限制来控制，但是递归是按照你想获得的结果来停止




